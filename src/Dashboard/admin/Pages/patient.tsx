import React from 'react';

// Define any props or state interfaces if needed. 
// For now, the component does not use any props or state.

const Pateints: React.FC = () => {
  return (
    <div>
      Pateints
    </div>
  );
};

export default Pateints;
